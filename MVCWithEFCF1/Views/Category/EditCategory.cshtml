@model MVCWithEFCF1.Models.Category

@{
    ViewBag.Title = "EditCategory";
}

<h2>EditCategory</h2>

@using (Html.BeginForm("UpdateCategory", "Category"))
{
    <div>
        @Html.LabelFor(C => C.CategoryId)@Html.TextBoxFor(C => C.CategoryId, new { @readonly = "true" })
    </div>


    <div>
        @Html.LabelFor(C => C.CategoryName)
        @Html.TextBoxFor(c => c.CategoryName)
    </div>
    <div>
        @Html.LabelFor(C => C.Description)
        @Html.TextBoxFor(c => c.Description)
    </div>
    <div>
        <input type="submit" value="Save" name="btnSave" />
        @Html.ActionLink("Cancel", "DisplayCategories")
    </div>




}